# First-and-Last-Characters-Extractor
A Python program that extracts the first two and last two characters from a user-provided word and combines them into a new string. The project demonstrates string slicing, functions, user input handling, and string manipulation techniques.
